<UserControl x:Class="Mailclient.Reply"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mailclient"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             FontFamily="Segoe UI"
             d:DesignHeight="300" 
             d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#C3E7FF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#AACCFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#80B3FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TransparentButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#5f6368"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F3F4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect Direction="270" ShadowDepth="0" Color="#B0B0B0" Opacity="0.5" BlurRadius="15" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#F1F3F4" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6*"/>
                        <ColumnDefinition Width="103*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="To" TextAlignment="Right" Width="22" FontSize="14" Margin="5,5,5,5" Background="White"/>
                    <TextBox x:Name="To" Grid.Column="1" BorderThickness="0" FontSize="14" Margin="5,5,5,5" VerticalContentAlignment="Center" TextWrapping="Wrap" Grid.ColumnSpan="2"
                             Text="{Binding FromUser}"/>
                </Grid>
            </Border>

            <wv2:WebView2 x:Name="EditorWebView" Grid.Row="1" Margin="10"/>

            <ListBox x:Name="lbAttachments" Grid.Row="2" MaxHeight="120" BorderThickness="0" Background="Transparent" Margin="10,5" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F5F5F5" CornerRadius="4" Padding="10,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FileName}" FontWeight="Bold" Foreground="#0b57d0" TextTrimming="CharacterEllipsis" MaxWidth="350"/>
                                    <TextBlock Text="{Binding FileSize}" Foreground="#5f6368" Margin="5,0,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="✕" Click="RemoveAttachment_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Foreground="#757575" FontWeight="Bold" Cursor="Hand" Padding="5,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="4" Background="#EAF1FB" Padding="10" CornerRadius="0,0,8,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Send" 
                            Grid.Column="0" 
                            Width="80" 
                            Click="Send_Click" 
                            Style="{StaticResource PrimaryButton}" 
                            HorizontalAlignment="Left"
                            Margin="0,0,10,0"/>

                    <Button x:Name="attachment" 
                            Grid.Column="1" 
                            Style="{StaticResource PrimaryButton}" 
                            Content="&#xE16C;" 
                            Click="opfile" 
                            FontSize="18" 
                            FontFamily="Segoe MDL2 Assets"
                            Width="40"
                            Margin="0,0,5,0"/>

                    <Button x:Name="btnInsertImage" 
                            Grid.Column="2" 
                            Style="{StaticResource PrimaryButton}" 
                            Width="40" 
                            ToolTip="Chèn ảnh vào nội dung" 
                            Content="&#xE8B9;" 
                            Click="InsertImage_Click" 
                            FontSize="17" 
                            FontFamily="Segoe MDL2 Assets"/>

                    <Button x:Name="btnClose"
                            Grid.Column="4"
                            Style="{StaticResource TransparentButton}"
                            Content="&#xE74D;"
                            ToolTip="Hủy thư trả lời"
                            Click="closecompose"
                            FontSize="16"
                            Width="40"
                            FontFamily="Segoe MDL2 Assets"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>