<UserControl x:Class="Mailclient.Forward"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mailclient"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             FontFamily="Segoe UI"
             d:DesignHeight="350" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#C3E7FF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#AACCFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#80B3FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TransparentButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#5f6368"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F3F4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="12" BorderBrush="#dadce0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Direction="270" ShadowDepth="2" Color="#000" Opacity="0.1" BlurRadius="10" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#F1F3F4" BorderThickness="0,0,0,1" Padding="15,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="To" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox x:Name="txtTo" Grid.Column="1" BorderThickness="0" FontSize="14" 
                             VerticalContentAlignment="Center" Background="Transparent"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <wv2:WebView2 x:Name="EditorWebView" Margin="2"/>
            </Grid>

            <Grid Grid.Row="2" Margin="10,5,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="Send" 
                            Grid.Column="0" 
                            Width="80" 
                            Click="Send_Click" 
                            Style="{StaticResource PrimaryButton}" 
                            HorizontalAlignment="Left"
                            Margin="0,0,10,0"/>

                <Button x:Name="attachment" 
                            Grid.Column="1" 
                            Style="{StaticResource PrimaryButton}" 
                            Content="&#xE16C;" 
                            Click="opfile" 
                            FontSize="18" 
                            FontFamily="Segoe MDL2 Assets"
                            Width="40"
                            Margin="0,0,5,0"/>

                <Button x:Name="btnInsertImage" 
                            Grid.Column="2" 
                            Style="{StaticResource PrimaryButton}" 
                            Width="40" 
                            ToolTip="Chèn ảnh vào nội dung" 
                            Content="&#xE8B9;" 
                            Click="InsertImage_Click" 
                            FontSize="17" 
                            FontFamily="Segoe MDL2 Assets"/>

                <Button x:Name="btnClose"
                            Grid.Column="4"
                            Style="{StaticResource TransparentButton}"
                            Content="&#xE74D;"
                            ToolTip="Hủy thư trả lời"
                            Click="closeforward"
                            FontSize="16"
                            Width="40"
                            FontFamily="Segoe MDL2 Assets"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>