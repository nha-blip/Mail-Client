<UserControl x:Class="Mailclient.Compose"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mailclient"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             FontFamily="Segoe UI"
             Height="400" Width="500">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">

            <Setter Property="Background" Value="#C3E7FF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10"/>

            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="border" 
                        CornerRadius="8" 
                        Background="{TemplateBinding Background}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        >

                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        </Border>
                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#AACCFF"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#80B3FF"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="White" 
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect Direction="270" 
                  ShadowDepth="0" 
                  Color="#B0B0B0" 
                  Opacity="0.5" 
                  BlurRadius="15" />
        </Border.Effect>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>

                <RowDefinition Height="Auto"/>

                <RowDefinition Height="Auto"/>

                <RowDefinition Height="*"/>

                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#EAF1FB" 
                    Padding="10"
                    MouseLeftButtonDown="maximize"
                    CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="249*"/>
                        <ColumnDefinition Width="191*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                    Text="New Message" 
                    FontWeight="Bold" Grid.ColumnSpan="2"/>
                    <Button Grid.Column="2"
                            Content="&#xE921;"
                            FontFamily="Segoe MDL2 Assets" 
                            Background="Transparent"
                            Foreground="Black"
                            Width="20"
                            FontSize="10"
                            Click="Minimize"
                            BorderThickness="0"
                            />
                    <Button Grid.Column="3"
                            Content="&#xE8BB;"
                            FontFamily="Segoe MDL2 Assets" 
                            Background="Transparent"
                            Foreground="Black"
                            Width="20"
                            FontSize="10"
                            Click="closecompose"
                            BorderThickness="0"/>
                </Grid>
            </Border>
            <Border Grid.Row="1" BorderBrush="#F1F3F4" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="To" 
                       TextAlignment="Center"
                       Width="50"
                       FontSize="14"
                           Margin="5"
                       Background="White"/>

                    <TextBox x:Name="To"
                        Grid.Column="1" 
                         BorderThickness="0"
                         FontSize="14"
                         Margin="5"
                         VerticalContentAlignment="Center"
                         TextWrapping="Wrap"/>
                </Grid>
            </Border>
            <Border Grid.Row="2" BorderBrush="#F1F3F4" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="24*"/>
                        <ColumnDefinition Width="61*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Subject" 
                           TextAlignment="Center"
                           Width="50"
                           Margin="5"
                           FontSize="14"
                            Background="White"/>
                    <TextBox x:Name="Subject"
                        Grid.Column="1" 
                         BorderThickness="0" 
                         Margin="5,5,5,5"
                         FontSize="14"
                         VerticalContentAlignment="Center"
                         TextWrapping="Wrap" Grid.ColumnSpan="2"/>
                </Grid>
            </Border>

            <wv2:WebView2 x:Name="EditorWebView" Grid.Row="3" Margin="10"/>

            <ListBox x:Name="lbAttachments" Grid.Row="4" 
         MaxHeight="120" 
         BorderThickness="0" 
         Background="Transparent" 
         Margin="10,5"
         ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F5F5F5" CornerRadius="4" Padding="10,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FileName}" 
                                   FontWeight="Bold" 
                                   Foreground="#0b57d0" 
                                   TextTrimming="CharacterEllipsis" 
                                   MaxWidth="350"/>

                                    <TextBlock Text="{Binding FileSize}" 
                                   Foreground="#5f6368" 
                                   Margin="5,0,0,0"/>
                                </StackPanel>

                                <Button Grid.Column="1" 
                            Content="✕" 
                            Click="RemoveAttachment_Click" 
                            Tag="{Binding}"
                            Background="Transparent" 
                            BorderThickness="0" 
                            Foreground="#757575" 
                            FontWeight="Bold"
                            Cursor="Hand"
                            Padding="5,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="5" Background="#EAF1FB" Padding="10" CornerRadius="0,0,8,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Send" 
                            Grid.Column="0"
                        Width="80" 
                            Click="Send_Click"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Left"/>
                    <Button x:Name="attachment"
                            Grid.Column="1"
                            Style="{StaticResource PrimaryButton}"
                            Content="&#xE16C;"
                            Click="opfile"
                            FontSize="18"
                            FontFamily="Segoe MDL2 Assets"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
